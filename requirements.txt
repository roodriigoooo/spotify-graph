# Local development / running handlers outside Lambda.
#
# At deploy these come from two places, NOT from here:
#   - boto3/botocore  : provided by the python3.12 Lambda runtime
#   - requests, PyJWT : the shared CommonLayer (layers/common/requirements.txt)
# The taste engine itself is pure stdlib and needs nothing.
boto3==1.34.14
requests==2.31.0
PyJWT==2.8.0
